# Simple Experiment with oblique project with 2d canvas

## Note
All commands assume `./node_modules/.bin/` is in your path

## build
```
npm install
npm run build
```

## view
```
npm start
```

then browse to http://localhost:3000
